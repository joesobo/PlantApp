export type RootStackParamList = {
  Home: undefined;
  PlantInfo: undefined;
};

export type Task = {
  title?: string;
  description?: string;
  waterIncrement: number;
  image: string;
};

export type Colors = {
  shadow: string;
  background: string;
  waterColor: string;
  descText: string;
  titleText: string;
  buttonText: string;
  text: string;
  border: string;
};
